[phases.setup]
nixPkgs = ["php83", "php83Extensions.pdo_mysql", "php83Extensions.mysqli", "php83Extensions.curl"]

[phases.install]
cmds = ["echo 'PHP Ready'"]

[phases.start]
cmd = "php -S 0.0.0.0:8080 -t ."

[metadata]
watch = ["*.php", "templates/"]